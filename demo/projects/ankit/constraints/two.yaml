apiVersion: opa.k8smgmt.io/v3
kind: Constraint
metadata:
  labels:
    rafay.dev/opa: constraint
  name: two
  project: ankit
spec:
  artifact:
    artifact:
      paths:
      - name: file://artifacts/two/container-must-meet-ratio1.yaml
    options: {}
    type: Yaml
  templateName: one
